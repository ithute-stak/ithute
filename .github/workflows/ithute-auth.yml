name: Ithute Auth

on:
  pull_request:
    paths:
      - "platform/ithute-auth/**"
      - ".github/workflows/ithute-auth.yml"
  push:
    branches: [main]
    paths:
      - "platform/ithute-auth/**"
      - ".github/workflows/ithute-auth.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  auth-contract:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: platform/ithute-auth
    steps:
      - name: Checkout repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          persist-credentials: false
      - name: Set up Python 3.12
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: platform/ithute-auth/requirements.txt
      - name: Show Python
        run: python3 --version
      - name: Install dependencies
        run: python3 -m pip install --disable-pip-version-check -r requirements.txt
      - name: Compile service
        run: python3 -m compileall -q app alembic tests
      - name: Run auth contract tests
        run: python3 -m pytest -q
