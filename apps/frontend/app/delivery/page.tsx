import { Server } from "lucide-react";
import { FutureModule } from "@/components/future-module";
export default function Page(){return <FutureModule title="Delivery & queues" subtitle="SMTP queues, bounces, delivery traces and Rspamd operational controls arrive with the mail infrastructure and deliverability phases." phase="Phases 6–8" icon={Server}/>;}
