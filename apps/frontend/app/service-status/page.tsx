"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {Activity,CheckCircle2} from "lucide-react";const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8006/api/v1";type Incident={id:string;title:string;summary:string;status:string;impact:string;started_at?:string|null};export default function ServiceStatusPage(){const[data,setData]=useState<{status:string;incidents:Incident[]}|null>(null);useEffect(()=>{void fetch(`${API}/public/status`).then(r=>r.json()).then(setData).catch(()=>setData({status:"unknown",incidents:[]}))},[]);const operational=data?.status==="operational";return <main className="min-h-screen bg-[#f4f6f4] text-[#21342a]"><header className="bg-[#123a38] text-white"><div className="mx-auto flex max-w-[980px] items-center justify-between px-5 py-5"><Link href="/" className="font-black">Mailbox DNS</Link><Link href="/login" className="text-xs font-bold text-white/70">Administration</Link></div></header><section className="mx-auto max-w-[980px] px-5 py-14"><p className="text-[10px] font-black uppercase tracking-[.14em] text-[#718078]">Public service status</p><div className={`mt-4 flex items-center gap-4 rounded-2xl border p-5 ${operational?"border-emerald-200 bg-emerald-50":"border-amber-200 bg-amber-50"}`}>{operational?<CheckCircle2 className="text-emerald-600"/>:<Activity className="text-amber-700"/>}<div><h1 className="text-xl font-black">{operational?"All systems operational":(data?.status||"Checking service status").replaceAll("_"," ")}</h1><p className="mt-1 text-xs text-[#617168]">Mail, authoritative DNS and the control plane are tracked here for customer-facing incidents.</p></div></div><div className="mt-8"><h2 className="text-sm font-black">Incident history</h2>{data?.incidents.length?<div className="mt-3 grid gap-3">{data.incidents.map(i=><article key={i.id} className="rounded-2xl border border-[#dfe6e2] bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="font-black">{i.title}</h3><span className="rounded-full bg-[#edf3f0] px-2.5 py-1 text-[9px] font-black uppercase">{i.impact} · {i.status}</span></div><p className="mt-2 text-xs leading-5 text-[#617168]">{i.summary}</p><p className="mt-3 text-[9px] text-[#8a9890]">{i.started_at?new Date(i.started_at).toLocaleString():""}</p></article>)}</div>:<div className="mt-3 rounded-2xl border border-[#dfe6e2] bg-white p-6 text-sm text-[#718078]">No incidents have been published.</div>}</div></section></main>}
