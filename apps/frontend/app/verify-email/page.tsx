"use client";import Link from "next/link";import {useEffect,useState} from "react";const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8006/api/v1";
export default function VerifyEmail(){const[state,setState]=useState("Verifying your email…");const[ok,setOk]=useState(false);useEffect(()=>{void(async()=>{const token=new URLSearchParams(window.location.search).get("token");if(!token){setState("Verification token is missing.");return}const r=await fetch(`${API}/public/email-verification/verify`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token})});const p=await r.json().catch(()=>({}));setOk(r.ok);setState(r.ok?"Your email has been verified. You can now sign in.":String(p.detail||"Verification failed or expired."))})()},[]);return <main className="grid min-h-screen place-items-center bg-[#f4f6f4] p-5"><div className="w-full max-w-lg rounded-3xl border bg-white p-8 text-center shadow-sm"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#123a38] font-black text-[#d8c56a]">MD</div><h1 className="mt-5 text-2xl font-black">Email verification</h1><p className={`mt-3 text-sm ${ok?"text-emerald-700":"text-[#718078]"}`}>{state}</p><Link href="/login" className="mt-6 inline-flex rounded-xl bg-[#123a38] px-5 py-3 text-sm font-black text-white">Go to sign in</Link></div></main>}
