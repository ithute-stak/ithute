apiVersion: 1
providers:
  - name: Mailbox DNS Operations
    orgId: 1
    folder: Mailbox DNS
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
