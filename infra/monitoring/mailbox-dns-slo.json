{
  "annotations": {"list": []},
  "editable": true,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "type": "stat",
      "title": "7d Availability",
      "targets": [{"expr": "mailbox_dns:slo_availability_7d", "refId": "A"}],
      "fieldConfig": {"defaults": {"unit": "percentunit", "min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0}
    },
    {
      "type": "stat",
      "title": "Error Budget Remaining",
      "targets": [{"expr": "mailbox_dns:slo_error_budget_remaining_7d", "refId": "A"}],
      "fieldConfig": {"defaults": {"unit": "percentunit", "min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0}
    },
    {
      "type": "stat",
      "title": "1h Error Budget Burn Rate",
      "targets": [{"expr": "mailbox_dns:slo_burn_rate_1h", "refId": "A"}],
      "fieldConfig": {"defaults": {"unit": "none", "min": 0}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Synthetic Service Availability",
      "targets": [
        {"expr": "probe_success{job=~\"smtp-synthetic|imap-synthetic|dns-synthetic\"}", "legendFormat": "{{job}}", "refId": "A"}
      ],
      "fieldConfig": {"defaults": {"min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 9, "w": 12, "x": 0, "y": 8}
    },
    {
      "type": "timeseries",
      "title": "Control Plane 5xx Ratio",
      "targets": [
        {"expr": "sum(rate(mailbox_dns_http_requests_total{status=~\"5..\"}[5m])) / clamp_min(sum(rate(mailbox_dns_http_requests_total[5m])), 0.001)", "refId": "A"}
      ],
      "fieldConfig": {"defaults": {"unit": "percentunit", "min": 0}, "overrides": []},
      "gridPos": {"h": 9, "w": 12, "x": 12, "y": 8}
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["mailbox-dns", "slo", "operations"],
  "templating": {"list": []},
  "time": {"from": "now-24h", "to": "now"},
  "timezone": "browser",
  "title": "Mailbox DNS SLO & Error Budget",
  "uid": "mailbox-dns-slo",
  "version": 1
}
