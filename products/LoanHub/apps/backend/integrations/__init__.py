"""Provider adapters for optional enterprise integrations.

The core origination workflow is independent from these adapters. Live calls are
only enabled after a tenant stores approved credentials and turns the provider on.
"""
