# Development and CI-only dependencies.
# Keep runtime dependencies in requirements.txt so the production image stays lean.
pytest>=9.1.1,<10.0
pytest-cov>=7.1.0,<8.0
pytest-asyncio>=1.4.0,<2.0
httpx>=0.28.1,<1.0
bandit>=1.9.4,<2.0
playwright>=1.62.0,<2.0
