"use client";import { useEffect,useState } from "react";import { professionalApi } from "@/api/professional";import { toast } from "@/utils/toast";export default function Page(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{professionalApi.wall().then(setRows)},[]);return <main className="space-y-6 p-6"><div><h1 className="text-3xl font-black">Loan offers wall</h1><p className="text-muted-foreground">Published offers from participating lenders.</p></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{rows.map(r=><article key={r.id} className="rounded-3xl border bg-card p-5"><span className="text-xs font-black uppercase text-primary">{r.company_name}</span><h2 className="mt-2 text-xl font-black">{r.title}</h2><p className="mt-2 text-sm text-muted-foreground">{r.summary}</p><button onClick={async()=>{await professionalApi.interest(r.id);toast.success("Interest sent to the lender")}} className="mt-4 h-11 w-full rounded-xl bg-primary font-black text-primary-foreground">I am interested</button></article>)}</div></main>}
