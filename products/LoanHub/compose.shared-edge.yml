# The shared Ithute edge resolves these stable aliases to the current LoanHub
# containers; the production workflow reloads the edge after replacements.
services:
  backend:
    networks:
      loanhub:
        aliases:
          - loanhub-backend

  frontend:
    networks:
      loanhub:
        aliases:
          - loanhub-frontend
