"use client";import{configureStore,createSlice}from"@reduxjs/toolkit";const live=createSlice({name:"live",initialState:{connected:false,revision:0,lastEvent:null as any},reducers:{setConnected:(s,a)=>{s.connected=a.payload},event:(s,a)=>{s.lastEvent=a.payload;s.revision++}}});export const{setConnected,event}=live.actions;export const store=configureStore({reducer:{live:live.reducer}});export type RootState=ReturnType<typeof store.getState>;
